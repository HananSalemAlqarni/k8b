apiVersion: v1
kind: Pod
metadata:
  name: images-day1-assignment
  labels:
    app: myFirstPod
    type: network
spec:
  containers:
    - name: nginx
      image: nginx:alpine
    - name: redis
      image: redis
    - name: memcached
      image: memcached