apiVersion: v1
kind: Pod
metadata:
  name: 3-images-Day1-Assignment-1
  labels:
    app: myFirstPod
    type: network
spec:
  containers:
    - name: nginx
      image: nginx:alpine
    - name: redis
      image: redis
    - name: memcached
      image: memcached